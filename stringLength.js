const stringLength = (str) => {
if(str.length > 1 && str.length <= 10) {
  return str.length;
}else{
   return 'error';
};
};

module.exports = stringLength;


const reverseString = () =>{
 return str.reverse();
}
console.log(reverseString("retry"))